export type TechInterview = {
  id: number;
  language: string;
  question: string;
  answer: string;
  notes: string;
  createdAt: Date;
  updatedAt: Date;
};

const InterviewQuestions: React.FC = () => {
  // const { data, isLoading, isError } = useGetTechInterviewsQuery();

  return <></>;
};

export default InterviewQuestions;
